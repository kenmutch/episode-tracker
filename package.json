{
    "name": "episode-tracker",
    "description": "Track availability of episodes within selected shows, and trigger their download",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "aws-sdk": "^2.245.1",
        "bluebird": "^3.5.1",
        "bunyan": "^1.8.12"
    },
    "devDependencies": {
        "fs-extra": "^6.0.1",
        "json-templates": "^2.0.0",
        "mocha": "5.0.0",
        "should": "^13.2.1",
        "supertest": "3.0.0",
        "unit.js": "2.0.0"
    },
    "scripts": {
        "test": "mocha --recursive tests",
        "pre-commit": "npm test && npm run e2e:local",
        "e2e:local": "./ci/e2e-local.sh"
    }
}
